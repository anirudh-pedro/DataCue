console.warn('The Node-based OTP service has been replaced by the FastAPI implementation.');
console.warn('Update your workflow to call the Python backend (/otp) endpoints instead.');
process.exit(1);
